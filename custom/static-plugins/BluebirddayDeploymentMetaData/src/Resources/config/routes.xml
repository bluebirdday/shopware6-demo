<?xml version="1.0" encoding="UTF-8" ?>
<routes xmlns="http://symfony.com/schema/routing"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://symfony.com/schema/routing
        https://symfony.com/schema/routing/routing-1.0.xsd">

    <!-- Use annotations for routing -->
    <import resource="../../Controller/*Controller.php" type="annotation" />
    <import resource="../../Controller/Api/*Controller.php" type="annotation"/>

    <!--
        Use a configured route in this XML
        But looks like we are missing the route scopes option
    -->
    <!--
    <route id="frontend.bluebirdday.deployment_info" path="/deployment-info"
           controller="Bluebirdday\DeploymentMetaData\Controller\IndexController::index" methods="GET">
        <option key="seo">false</option>
    </route>
    -->
</routes>
